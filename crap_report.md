# CRAP Index Report
This report highlights methods with the highest CRAP scores across all services.

CRAP(m) = comp(m)^2 * (1 - cov(m))^3 + comp(m)
where `comp` is cyclomatic complexity and `cov` is instruction coverage.

